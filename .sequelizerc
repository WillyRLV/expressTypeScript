const path = require('path');

module.exports = {
  config: path.resolve('./src/config', 'config.json'), // O .mjs si usas ES Modules
  'models-path': path.resolve('./src/models'),
  'seeders-path': path.resolve('./src/seeders'), // Opcional, si usas seeders
  'migrations-path': path.resolve('./src/migrations')
};